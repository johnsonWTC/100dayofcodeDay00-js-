@model SimpleImageGallary.Models.GalleryIndexModel 

<div class="container body-content">
    <div class="gallery-content">
        @foreach (var image in Model.Images)
        {
            <div class="gallery-image"></div>
            <h5> @image.url with @image.Title </h5>
        }
    </div>



</div>